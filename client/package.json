{
  "name": "deadzone-client",
  "version": "1.0.0",
  "description": "Client for Deadzone v2 multiplayer FPS",
  "main": "public/client.js",
  "scripts": {
    "start": "vercel dev",
    "build": "vercel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "socket.io-client": "^4.7.2",
    "three": "^0.150.0"
  },
  "devDependencies": {
    "vercel": "^32.4.1"
  }
}